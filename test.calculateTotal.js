const assert = require('assert');
const calculateTotalPoints = require('./calculateTotal');

describe('calculateDTotalPoints', () => {
  describe('seconds', () => {
      it('should return seconds', () => {
      });
  });
});
